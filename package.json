{
  "name": "univault",
  "version": "1.0.0",
  "description": "Decentralized credential management system with DID anchoring",
  "main": "index.js",
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "accounts": "hardhat run scripts/get-accounts.js --network localhost",
    "test": "hardhat test"
  },
  "keywords": [
    "blockchain",
    "credentials",
    "DID",
    "ethereum",
    "hardhat"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "ethers": "^6.13.2"
  }
}
